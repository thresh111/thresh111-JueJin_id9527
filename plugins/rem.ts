export default defineNuxtPlugin(() => {
  // if (process.client) {
  //   // 屏幕适应
  //   (function (win, doc) {
  //     if (!win.addEventListener) { return }
  //     function setWinds () {
  //       const width = document.body.offsetWidth
  //       if (width < 768) { }
  //       else if (width >= 768 && width < 1200) { }
  //       else if (width > 1200) { }
  //       else { }
  //     }
  //     setWinds()
  //     setTimeout(() => {
  //       setWinds()
  //     }, 300)
  //     doc.addEventListener('DOMContentLoaded', setWinds)
  //     win.addEventListener('resize', setWinds)
  //     win.addEventListener('load', setWinds)
  //   })(window, document)
  // }
})
